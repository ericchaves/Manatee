{ 
  "pk": "int PRIMARY KEY IDENTITY(1,1)",
  "money": "decimal(8,2)",
  "date": "datetime",
  "string": "nvarchar(255)",
  "boolean": "bit",
  "text": "nvarchar(MAX)",
  "guid": "uniqueidentifier"
}
